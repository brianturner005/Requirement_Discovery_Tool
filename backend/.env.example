DATABASE_URL=sqlite+aiosqlite:///./data/requirements.db
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=25
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
APP_VERSION=0.2.0
SECRET_KEY=change-me-in-production-use-a-long-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# --- Production (Vercel) overrides — set these in the Vercel dashboard, not in this file ---
# DATABASE_URL=postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DBNAME?sslmode=require
# STORAGE_BACKEND=vercel_blob
# BLOB_READ_WRITE_TOKEN is injected automatically once a Blob store is linked to the project
